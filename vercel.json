{
  "buildCommand": "pnpm install --filter=@dripl/common --filter=@dripl/db --filter=@dripl/math --filter=@dripl/utils --filter=@dripl/element && pnpm build",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
