{
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "routes": [
          { "src": "/(.*)", "dest": "/api/index" }
        ]
      }
    }
  ],
  "installCommand": "cd ../.. && bash scripts/vercel-install.sh http-server"
}
