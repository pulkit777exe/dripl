{
  "name": "@dripl/db",
  "version": "0.0.0",
  "description": "Database layer with Prisma ORM for Dripl",
  "license": "MIT",
  "private": true,
  "main": "./dist/index",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index"
    }
  },
  "type": "module",
  "scripts": {
    "build": "node -e \"const { execSync } = require('child_process'); let s = false; for (let i = 0; i < 5; i++) { try { execSync('npx prisma generate', { stdio: 'inherit' }); s = true; break; } catch(e) { console.log('Retrying prisma generate...'); } } if (!s) process.exit(1);\" && tsc -b --force",
    "lint": "eslint .",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.0.1",
    "@prisma/client": "^7.2.0",
    "dotenv": "^17.2.3",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@dripl/eslint-config": "workspace:*",
    "@dripl/typescript-config": "workspace:*",
    "@types/node": "^25.5.2",
    "@types/pg": "^8.11.0",
    "eslint": "^9.0.0",
    "prisma": "^7.2.0",
    "typescript": "^5.0.0"
  }
}
